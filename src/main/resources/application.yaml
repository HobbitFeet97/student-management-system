spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/student-management
    username: root
    password: PASSWORD
    driver-class-name: com.mysql.cj.jdbc.Driver

auth:
  username: USERNAME
  password: PASSWORD
